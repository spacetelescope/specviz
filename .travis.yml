language: python
os:
  - linux
  - osx
python:
  - "2.7"
  - "3.5"
  - "3.6"
addons:
  apt:
    packages:
      - graphviz
      - texlive-latex-extra
      - dvipng
install:
  - "pip install -r requirements.txt"
script:
   - sphinx-build -nW -b html -d _build/doctrees . _build/html
   - pytest